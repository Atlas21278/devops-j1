<?xml version='1.1' encoding='UTF-8'?>
<project>
  <description>Déploiement de l'application todolist sur le serveur de test (app-dev) via Ansible</description>
  <keepDependencies>false</keepDependencies>

  <properties/>

  <!-- Clone le repo devops contenant le playbook Ansible -->
  <scm class="hudson.plugins.git.GitSCM" plugin="git">
    <configVersion>2</configVersion>
    <userRemoteConfigs>
      <hudson.plugins.git.UserRemoteConfig>
        <url>https://github.com/Atlas21278/devops-j1.git</url>
      </hudson.plugins.git.UserRemoteConfig>
    </userRemoteConfigs>
    <branches>
      <hudson.plugins.git.BranchSpec>
        <name>master</name>
      </hudson.plugins.git.BranchSpec>
    </branches>
    <doGenerateSubmoduleConfigurations>false</doGenerateSubmoduleConfigurations>
    <submoduleCfg class="empty-list"/>
    <extensions/>
  </scm>

  <triggers/>
  <concurrentBuild>false</concurrentBuild>

  <builders>
    <hudson.tasks.Shell>
      <command>
#!/bin/bash
set -e

echo "=== Déploiement sur app-dev ==="
ansible-playbook deploy_app.yml \
  -i inventory-prod.yaml \
  --limit app-dev
      </command>
    </hudson.tasks.Shell>
  </builders>

  <publishers/>
  <buildWrappers/>
</project>
